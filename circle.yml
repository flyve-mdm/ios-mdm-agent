machine:
  xcode:
    version: 8.3
compile:
  override:
    - xcodebuild clean -workspace flyve-mdm-ios.xcworkspace -scheme flyve-mdm-ios CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO
dependencies:
  override:
    - brew install node